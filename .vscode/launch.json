{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Edge & Run Full Stack",
      "type": "msedge",
      "request": "launch",
      // Update this if your React app runs on a different port!
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/frontend",
      "preLaunchTask": "Run Full Stack"
    }
  ]
}
